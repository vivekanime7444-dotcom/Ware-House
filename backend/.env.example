# Server Configuration
PORT=8000
HOST=0.0.0.0

# Database Configuration (SQLite default, PostgreSQL optional)
DATABASE_URL=sqlite:///./simulator.db
# DATABASE_URL=postgresql://user:password@localhost:5432/physics_db

# AI Provider Configuration
# Supported providers: openai, gemini, anthropic, or rule-based fallback if blank
AI_PROVIDER=fallback
AI_API_KEY=
